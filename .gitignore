# Composer
/vendor/
# NOTE: composer.lock is INTENTIONALLY committed for this plugin.
# The plugin is published as a release zip (not as a library that
# downstream projects require via composer), so pinning the exact
# dependency tree makes installations reproducible.

# Node (only used for any future stencil-bundling tooling)
/node_modules/
npm-debug.log
yarn-error.log

# CI-only scratch dir for the contract-validation job
# (see .github/workflows/ci.yml -> job "contract"). Must not be committed.
/ci-contract-tools/
/ci-lint-tools/
/sampleData.json

# Build output (onRender extraction target for the JS lint job).
/build/

# Build artifacts
/dist/*.zip
/dist/*.tar.gz

# PHP tooling caches
/.phpunit.cache/
/.php-cs-fixer.cache
/.phpstan.cache

# Coverage
/coverage/
/clover.xml
/phpunit.xml

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.log

# Env
.env
.env.*
!.env.example
