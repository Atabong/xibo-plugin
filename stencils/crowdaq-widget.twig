{#
 # Copyright (C) 2026 CROWDAQ
 # Licensed under AGPL-3.0-or-later. See the LICENSE file at the repo root.
 #
 # TODO (iter: "MVP CROWDAQ widget rendering"):
 #   Replace this placeholder with the real render tree. The MVP will bind
 #   to the CROWDAQ data-source contract defined in the `Define CROWDAQ
 #   data source contract` iter; expected variables will include at least
 #   `items` (array of cards/clips) and `display` (tags + metadata used
 #   for multi-bar targeting).
 #}
<div
    class="crowdaq-widget"
    data-widget-id="{{ widgetId|default('crowdaq-stub') }}"
    style="width:100%;height:100%;display:flex;align-items:center;justify-content:center;font-family:sans-serif;">
    <div class="crowdaq-placeholder">
        CROWDAQ placeholder &mdash; scaffold only.
    </div>
</div>
